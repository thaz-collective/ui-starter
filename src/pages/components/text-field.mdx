---
title: Text Field
date: 2026-07-15
author:
  - Tim Hazlett
---

# Text Field

A single- or multi-line text input built on top of `react-aria-components`' `TextField`,
styled with `tailwind-variants`.

<H2 id="import">Import</H2>

<pre>
  <code>{`import { TextField } from '#src/common/components/text-field';`}</code>
</pre>

<H2 id="composition">Composition</H2>

Use the following composition to build a `TextField`:

<pre>
  <code>{`TextField
├── TextField.LabelInputContainer (optional)
│   ├── TextField.Label (optional)
│   └── TextField.Input
├── TextField.Description (optional)
└── TextField.FieldError (optional)`}</code>
</pre>

<H2 id="anatomy">Anatomy</H2>

<table>
  <thead>
    <tr>
      <th>{'Component'}</th>
      <th>{'Description'}</th>
      <th>{'Optional'}</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>{'TextField / TextField.Root'}</td>
      <td>{'The container; supplies context to every slot below.'}</td>
      <td>{'No'}</td>
    </tr>
    <tr>
      <td>{'TextField.LabelInputContainer'}</td>
      <td>{'Wraps the label and input.'}</td>
      <td>{'Yes'}</td>
    </tr>
    <tr>
      <td>{'TextField.Label'}</td>
      <td>{"The field's label text."}</td>
      <td>{'Yes'}</td>
    </tr>
    <tr>
      <td>{'TextField.Input'}</td>
      <td>{'A single-line text input.'}</td>
      <td>{'No'}</td>
    </tr>
    <tr>
      <td>{'TextField.Description'}</td>
      <td>{'Supporting text under the field.'}</td>
      <td>{'Yes'}</td>
    </tr>
    <tr>
      <td>{'TextField.FieldError'}</td>
      <td>{'Validation error text, shown when isInvalid.'}</td>
      <td>{'Yes'}</td>
    </tr>
  </tbody>
</table>

Every slot must be rendered inside `TextField`/`TextField.Root` — they read shared
context and throw if used standalone.

<H2 id="examples">Examples</H2>

<H3 id="default">Default</H3>

<Preview
  code={`export function TextFieldExample() {
  const [value, setValue] = useState('');

return (

<TextField className="w-64" value={value} onChange={setValue}>
<TextField.LabelInputContainer>
<TextField.Label>Full name</TextField.Label>
<TextField.Input placeholder="Jane Smith" />
</TextField.LabelInputContainer>
<TextField.Description>Used on your public profile.</TextField.Description>
</TextField>
);
}`}

>

  <TextFieldExample />
</Preview>

<H2 id="states">States</H2>

<H3 id="error">Error</H3>

<Preview
  code={`export function TextFieldWithErrorStateExample() {
  const [value, setValue] = useState('');

return (

<TextField className="w-64" value={value} onChange={setValue} isInvalid>
<TextField.LabelInputContainer>
<TextField.Label>Email</TextField.Label>
<TextField.Input placeholder="you@example.com" />
</TextField.LabelInputContainer>
<TextField.FieldError>Please enter a valid email address.</TextField.FieldError>
</TextField>
);
}`}

>

  <TextFieldWithErrorStateExample />
</Preview>

<H3 id="disabled">Disabled</H3>

<Preview
  code={`export function TextFieldWithDisabledStateExample() {
  const [value, setValue] = useState('jane@example.com');

return (

<TextField className="w-64" value={value} onChange={setValue} isDisabled>
<TextField.LabelInputContainer>
<TextField.Label>Email</TextField.Label>
<TextField.Input />
</TextField.LabelInputContainer>
</TextField>
);
}`}

>

  <TextFieldWithDisabledStateExample />
</Preview>

<H3 id="readonly">Readonly</H3>

<Preview
  code={`export function TextFieldWithReadonlyStateExample() {
  const [value, setValue] = useState('jane@example.com');

return (

<TextField className="w-64" value={value} onChange={setValue} isReadOnly>
<TextField.LabelInputContainer>
<TextField.Label>Email</TextField.Label>
<TextField.Input />
</TextField.LabelInputContainer>
<TextField.Description>Contact support to change this.</TextField.Description>
</TextField>
);
}`}

>

  <TextFieldWithReadonlyStateExample />
</Preview>

<H3 id="required">Required</H3>

<Preview
  code={`export function TextFieldWithRequiredStateExample() {
  const [value, setValue] = useState('');

return (

<TextField className="w-64" value={value} onChange={setValue} isRequired>
<TextField.LabelInputContainer>
<TextField.Label>Email</TextField.Label>
<TextField.Input placeholder="you@example.com" />
</TextField.LabelInputContainer>
<TextField.Description>Required to create your account.</TextField.Description>
</TextField>
);
}`}

>

  <TextFieldWithRequiredStateExample />
</Preview>

<H2 id="in-a-card">In a Card</H2>

<Preview
  code={`export function TextFieldInACardExample() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');

return (

<Card variant="default" className="w-80">
<Card.Header>
<Card.Title>Sign in</Card.Title>
<Card.Description>Enter your credentials to continue.</Card.Description>
</Card.Header>
<Card.Content className="flex flex-col gap-5">
<TextField value={email} onChange={setEmail} isRequired>
<TextField.LabelInputContainer>
<TextField.Label>Email</TextField.Label>
<TextField.Input type="email" placeholder="you@example.com" />
</TextField.LabelInputContainer>
</TextField>
<TextField value={password} onChange={setPassword} isRequired isInvalid>
<TextField.LabelInputContainer>
<TextField.Label>Password</TextField.Label>
<TextField.Input type="password" placeholder="••••••••" />
</TextField.LabelInputContainer>
<TextField.FieldError>Incorrect password. Please try again.</TextField.FieldError>
</TextField>
</Card.Content>
</Card>
);
}`}

>

  <TextFieldInACardExample />
</Preview>

<H2 id="api">API</H2>

<PropsTable
  rows={[
    { name: 'value', type: 'string', default: 'undefined' },
    { name: 'defaultValue', type: 'string', default: 'undefined' },
    { name: 'type', type: "'text' | 'email' | 'password' | 'search' | 'tel' | 'url'", default: "'text'" },
    { name: 'isRequired', type: 'boolean', default: 'false' },
    { name: 'isDisabled', type: 'boolean', default: 'false' },
    { name: 'isReadOnly', type: 'boolean', default: 'false' },
    { name: 'isInvalid', type: 'boolean', default: 'false' },
  ]}
/>
