---
title: Button
date: 2024-07-15
author:
  - Tim Hazlett
---

# Button

An accessible button built on top of `react-aria-components`' `Button`, styled with
`tailwind-variants`.

<H2 id="import">Import</H2>

<pre>
  <code>{`import { Button } from '#src/common/components/button';`}</code>
</pre>

<H2 id="variants">Variants</H2>

<H3 id="primary">Primary</H3>

<Preview code={`<Button variant="primary">Primary</Button>`}>
  <ButtonPrimaryExample />
</Preview>

<H3 id="secondary">Secondary</H3>

<Preview code={`<Button variant="secondary">Secondary</Button>`}>
  <ButtonSecondaryExample />
</Preview>

<H3 id="danger">Danger</H3>

<Preview code={`<Button variant="danger">Danger</Button>`}>
  <ButtonDangerExample />
</Preview>

<H3 id="success">Success</H3>

<Preview code={`<Button variant="success">Success</Button>`}>
  <ButtonSuccessExample />
</Preview>

<H3 id="warning">Warning</H3>

<Preview code={`<Button variant="warning">Warning</Button>`}>
  <ButtonWarningExample />
</Preview>

<H3 id="ghost">Ghost</H3>

<Preview code={`<Button variant="ghost">Ghost</Button>`}>
  <ButtonGhostExample />
</Preview>

<H3 id="outline">Outline</H3>

<Preview code={`<Button variant="outline">Outline</Button>`}>
  <ButtonOutlineExample />
</Preview>

<H2 id="sizes">Sizes</H2>

<H3 id="small">Small</H3>

<Preview code={`<Button size="sm">Small</Button>`}>
  <ButtonSmallExample />
</Preview>

<H3 id="medium">Medium</H3>

<Preview code={`<Button size="md">Medium</Button>`}>
  <ButtonMediumExample />
</Preview>

<H3 id="large">Large</H3>

<Preview code={`<Button size="lg">Large</Button>`}>
  <ButtonLargeExample />
</Preview>

Icon-only sizes pair with a square, icon-sized button and take a single icon child
(no visible label):

<H3 id="small-icon">Small Icon</H3>

<Preview code={`<Button size="smIcon" aria-label="Add"><Plus /></Button>`}>
  <ButtonSmallIconExample />
</Preview>

<H3 id="medium-icon">Medium Icon</H3>

<Preview code={`<Button size="mdIcon" aria-label="Add"><Plus /></Button>`}>
  <ButtonMediumIconExample />
</Preview>

<H3 id="large-icon">Large Icon</H3>

<Preview code={`<Button size="lgIcon" aria-label="Add"><Plus /></Button>`}>
  <ButtonLargeIconExample />
</Preview>

<H2 id="full-width">Full Width</H2>

<Preview code={`<Button fullWidth>Full Width</Button>`}>
  <ButtonFullWidthExample />
</Preview>

<H2 id="inverted">Inverted</H2>

`isInverted` swaps a variant's palette for one meant to sit on an inverted (dark)
background. Only `primary`, `secondary`, `danger`, `success`, and `warning` define
inverted styles — `ghost` and `outline` are unaffected by `isInverted`.

<H3 id="primary-inverted">Primary Inverted</H3>

<Preview code={`<Button variant="primary" isInverted>Primary</Button>`}>
  <ButtonPrimaryInvertedExample />
</Preview>

<H3 id="secondary-inverted">Secondary Inverted</H3>

<Preview code={`<Button variant="secondary" isInverted>Secondary</Button>`}>
  <ButtonSecondaryInvertedExample />
</Preview>

<H3 id="danger-inverted">Danger Inverted</H3>

<Preview code={`<Button variant="danger" isInverted>Danger</Button>`}>
  <ButtonDangerInvertedExample />
</Preview>

<H3 id="success-inverted">Success Inverted</H3>

<Preview code={`<Button variant="success" isInverted>Success</Button>`}>
  <ButtonSuccessInvertedExample />
</Preview>

<H3 id="warning-inverted">Warning Inverted</H3>

<Preview code={`<Button variant="warning" isInverted>Warning</Button>`}>
  <ButtonWarningInvertedExample />
</Preview>

<H2 id="api">API</H2>

<PropsTable
  rows={[
    {
      name: 'variant',
      type: "'primary' | 'secondary' | 'danger' | 'success' | 'warning' | 'ghost' | 'outline'",
      default: "'primary'",
    },
    { name: 'size', type: "'sm' | 'md' | 'lg' | 'smIcon' | 'mdIcon' | 'lgIcon'", default: "'md'" },
    { name: 'fullWidth', type: 'boolean', default: 'false' },
    { name: 'isInverted', type: 'boolean', default: 'false' },
  ]}
/>
